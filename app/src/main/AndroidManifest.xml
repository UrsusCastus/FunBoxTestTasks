<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.funboxtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ThemeMaterialLight" >
        <activity android:name="com.example.funboxtest.storefront.StoreFrontActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.example.funboxtest.backend.BackEndActivity"/>
        <activity android:name="com.example.funboxtest.backend.ItemProductEditActivity"/>
    </application>

</manifest>
